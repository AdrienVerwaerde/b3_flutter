// Fichier theme pour définir des éléments visuels

import 'package:flutter/widgets.dart';

const primaryColor = Color.fromARGB(255, 100, 100, 100);
const secondaryColor = Color.fromRGBO(254, 159, 193, 1);
